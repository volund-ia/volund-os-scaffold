# volund-os-scaffold

Base de aplicação usada pelos **Apps** do [VolundOS](https://os.volund.com.br):
Next.js (App Router) + Postgres, pronta para o agente construir em cima.

O conteúdo real chega no PR de inicialização. Este commit existe apenas para
criar a branch `main` como base de revisão.
